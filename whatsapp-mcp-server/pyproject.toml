[project]
name = "whatsapp-mcp-server"
version = "0.1.0"
description = "MCP server exposing a WhatsApp message store to LLM clients"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.6.0",
    "requests>=2.32.3",
]
